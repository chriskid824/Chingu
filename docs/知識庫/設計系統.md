# 設計系統(Design Token)

**現行標準:莫蘭迪暖色系 + Apple Health 風格。所有色彩必須引用 `AppColorsMinimal`(lib/core/theme/),禁止硬編碼。**

## ⚠️ 版本注意

`timeleft_pivot_plan.md` 第一次圓桌定的是 Apple 白/橘系(#FF6B35→#FF9500、#F5F5F7)——**已被取代**。現行以 `CLAUDE.md` + `app_colors_minimal.dart` 為準(莫蘭迪系)。看到橘色系規格請視為歷史文件。

## 現行色彩

| 用途 | 色碼 |
|---|---|
| 主色 | `#2E5364` 深莫蘭迪藍灰 |
| 點綴 | `#A64A25` 磚橘 / `#D67756→#E9967A` 蜜桃橘漸層 |
| 背景 | `#F7F9FB` 極淺灰藍 |

圓角:SM=8 / MD=16 / LG=24。詳細規格與檢查清單:`docs/roles/uiux_director.md`。

## 動畫規格(pivot plan 定義,仍有效)

- 頁面切換:easeOutCubic 350ms;卡片出現:底部滑入+淡入 400ms
- 按鈕點擊:縮至 0.95 回彈 150ms;解鎖翻牌:3D 翻轉 180° + Haptic(medium)
- 倒數圓環:SweepGradient,直徑 200pt、環寬 12pt
- 已導入:P0 全局動畫 + P1 Token 合規掃除(2c304e3)、CountdownRing 即時更新(1dd719a)、開場動畫 6 人圓桌(77a1ab1)

## 關鍵 Widget

`lib/screens/home/widgets/`:countdown_ring、invite_card、matching_card、companion_preview_card、restaurant_reveal_card、pending_review_card、booking_bottom_sheet。
幾何頭像:grep `GeometricAvatar`(未上傳照片者的抽象頭像,059e74e)。

## 慣例

- 新 UI 一律引用 `AppColorsMinimal`,PR 前 grep `Color(0x` 確認沒硬編碼
- 文案繁體中文;demo/ 目錄有 28 個元件展示檔(Widgetbook 式)可對照

## AI 檢索與驗證

- Token 定義:讀 `lib/core/theme/app_colors_minimal.dart`
- 違規掃描:grep `Color(0xFF` 於 `lib/screens/` 排除 core/theme
