# 開發進度與缺口

**截至 2026-07-11(main 分支)。勾選狀態以 git log 與實際程式為準,CLAUDE.md 的 Phase 區塊有部分落後於實況。**

## 2026-07-11 大修波(五路稽核 + 修復,未 commit 時以 working tree 為準)

修掉的重點(細節見各概念卡):
- **時區**:functions 內部牆鐘計算全錯 8 小時 → 新增 `taipeiTime.ts` 統一換算(createWeeklyEvent、bookWithValidation、weeklyScheduler、scheduledNotifications)
- **分桌**:可能產出 8~9 人桌 → chunkSizes 保證 5~7;性別偏好(男→男/女→女)硬分區實作(原本完全沒讀 diningPreference);未成桌者保留下週+獨立通知;group id 確定性(`{eventId}_tNN`)防重跑重複
- **評價/Match**:client 死碼結算刪除(必 permission-denied 害提交報錯)、失敗不再誤顯「評價完成」且保留選擇可重試、逾期提交會明確告知、onMutualMatch 改 create() 防雙倍副作用、autoSkip 加 72hr 判斷
- **聊天**:未讀數雙重遞增(client+CF 各加一次)→ CF 單一來源;未讀從不歸零 → 進出房 markRoomRead;Match 建房兩套 ID 規則 → 只留 CF;群聊歡迎訊息空白(message vs text 欄位)→ 統一 text;照片解鎖後群聊頭像永不更新 → 接 photosUnlocked/participantAvatars;系統訊息不再觸發垃圾推播;解鎖前群聊推播匿名化
- **報名**:server 端截止檢查(原本截止後會另建孤兒活動照扣票)、活動查詢進 transaction+確定性 event id 防重複建立、分桌後不可退出
- **首頁**:cancelled 活動不再顯示「配對中」、倒數對準最近(原本對到最遠)、pendingReview 不再蓋掉新一週狀態、companionPreviews 過濾自己
- **rules 硬化**:dinner_events update 限自己報名/取消、dinner_groups 限參與者改出席、dinner_reviews create 驗 reviewerId、chat_rooms create 收回 CF
- **UI/動效**:FlipCard 餐廳翻牌、BounceButton/AnimatedCounter/Skeleton/stagger 接線、27 處硬編碼色收斂、morandiPalette token
- 測試 116 → 128 全綠(補 P0:首頁狀態機、雙盲不變量、聊天權限閘門)

**部署備忘**:functions 與 firestore.rules 都有變更,要 `firebase deploy --only functions,firestore:rules` 才生效;`dinner_groups` 舊資料無 `completedAt`,autoSkip 對無此欄位者維持舊行為。

## 2026-07-12 實機回報修復波(build #64 → #65)

1. **報名「伺服器錯誤」**:bookWithValidation 改查詢形狀(city+eventDate 無 status)缺複合索引 → FAILED_PRECONDITION。已補 (city, eventDate) 索引並部署。**教訓:改任何 Firestore 查詢形狀,先對照 firestore.indexes.json**
2. **導航/開外部連結全滅**:AndroidManifest 缺 `<queries>`(https/geo/mailto),Android 11+ 的 canLaunchUrl 永遠 false + 程式靜默吞掉;連「強制更新導去商店」都是壞的。已加宣告,並把 canLaunchUrl gate 改為直接 launch + 失敗提示(restaurant_reveal_card / force_update_service / about_screen)
3. **登入登出殭屍狀態(潛伏舊 bug,非本次引入)**:全 app 只有 AuthGate 會依登入狀態換頁,但 onboarding/登出/強制登出用 `pushNamedAndRemoveUntil(false)` 把 AuthGate 拆出 stack → 登入成功停在登入頁、登出卡殭屍主畫面。修法:新增 `/auth-gate` 路由,登入成功與登出後一律導回 AuthGate;登出 dialog 改用 root navigatorKey(避開 context.mounted 競態);auth_service.signOut 改序列式(Google 清不掉不擋 Firebase 登出);auth_provider 登出失敗但 Firebase 已登出時強制重設狀態;FCM token 移除加 3s timeout
4. **聊天未讀不歸零**:歸零有寫入,但列表是一次性 get() 返回不重載 → 返回自動重載 + 停留期間未讀即時歸零(watchRoom 收進 ChatProvider 供測試 mock)
5. FlipCard 翻開後改單向(點按鈕邊緣不再把餐廳卡翻回封面)

## Phase 總覽

| Phase | 狀態 | 證據 |
|---|---|---|
| 1 瘦身與架構清理(移除 explore/matching、4 Tab) | ✅ 完成 | lib/screens/ 無 explore、matching |
| 2 資料結構重建(DinnerEvent/Group model、集合統一) | ✅ 完成 | bc12e5d 統一集合名稱 |
| 3 動態首頁儀表板 | ✅ **大致完成**(CLAUDE.md 未更新) | 62a7c02 首頁 5 狀態、059e74e 幾何頭像、1dd719a 倒數圓環、79a845c 邀請卡/翻牌 |
| 4 評價與私訊解鎖 | ✅ 大致完成 | 0e17086 評價系統重構、多輪 bugfix 至 49f409a |
| 5 UI 設計系統導入 | 🟡 進行中 | 2c304e3 P0/P1 掃除完成;個別頁面仍在逐頁 polish |
| 6 營運後台網站 | ❌ 未開始 | lib/screens/admin/ 空;僅 sendBroadcast 函數存在 |

近期 git 活動全是 fix/polish(評價轉圈、聊天輸入框、Seeder 權限、Google 重登入)→ 專案處於**功能完備、穩定性打磨期**,正朝上架準備(APP_STORE_*.md 已備妥)。

## 明確缺口

1. **營運後台**(Phase 6)整個未做 — 每週營運(看報名、調分組、指定餐廳、回填訂位)目前只能靠 debug_screen + Firestore Console
2. **付費/訂閱** — model 與 provider 骨架在,screens/subscription/ 空(MVP 本來就不含,勿誤做)
3. lib/ 內僅 2 個 TODO:`help_center_screen.dart`(搜尋)、`dinner_event_service.dart`(配對演算法優化)
4. 測試缺口(TESTING_CHECKLIST.md 的 P0):首頁 4 狀態切換、雙盲評價邏輯、聊天室權限閘門
5. Security Rules 上線前必辦:`users` 讀取收緊、`dinner_events/groups` update 欄位限制、`isDeveloper()` email 後門改 custom claim(見 [資料模型](資料模型.md))

## 大量殭屍遠端分支

origin 有 **300+ 個自動生成的 feature 分支**(notification/moments/explore/2fa/favorites…),多為早期 AI 批量開發遺留,其中 moments、explore、favorites 等主題**違反現行產品鐵律**——不要從這些分支撿程式碼回來。現行事實以 main 為準。

## 已知反覆踩雷區(修過 ≥2 次)

- 評價流程轉圈/死鎖/重複提交(2a1e4be→49f409a 共 5 輪)
- Seeder 與 firestore.rules 衝突(permission-denied,2438bd9/ef193a2)
- Google 登出後重登(22ceb85)
- 聊天輸入框排版(41e90d4/49f409a)

## AI 檢索與驗證

- 最新狀態:`git log --oneline -20`
- TODO 掃描:grep `TODO\|FIXME` 於 lib/
- 空目錄確認:`ls lib/screens/admin lib/screens/subscription`
